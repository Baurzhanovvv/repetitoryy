version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: landing-page
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    networks:
      - landing-network

networks:
  landing-network:
    driver: bridge
